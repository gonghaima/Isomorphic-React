import React from 'react';
import { connect } from 'react-redux';

const AppDisplay = () => {
    return <div>
        <h1>
            Ismorphic React
    </h1>
    </div>
};

export default AppDisplay;